cmake_minimum_required(VERSION 3.20)
project(pantheonsim VERSION 0.1.0 LANGUAGES C CXX)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_C_STANDARD 11)
set(CMAKE_POSITION_INDEPENDENT_CODE ON)
if(NOT CMAKE_BUILD_TYPE)
  set(CMAKE_BUILD_TYPE Release)
endif()
add_compile_options(-Wall -Wextra)

# The interpreter is the hot path and benefits measurably from cross-TU
# inlining. Enabled only for Release builds, and only if the toolchain
# supports it, so debug builds and odd toolchains are unaffected.
include(CheckIPOSupported)
check_ipo_supported(RESULT VGPU_HAVE_LTO OUTPUT _lto_msg)
if(VGPU_HAVE_LTO AND CMAKE_BUILD_TYPE STREQUAL "Release")
  set(CMAKE_INTERPROCEDURAL_OPTIMIZATION TRUE)
  message(STATUS "Link-time optimization enabled for Release")
endif()

# ---------------------------------------------------------------------------
# Embed device profiles (profiles/*.yaml) into a generated header so binaries
# are self-contained. Re-runs automatically when a profile file changes.
# ---------------------------------------------------------------------------
set(VGPU_PROFILES
  nvidia/a10
  nvidia/a100
  nvidia/h100
  nvidia/h200
  nvidia/b200
  nvidia/rtx3060
  nvidia/a100-sxm4-40gb
  nvidia/gh200-480gb
  nvidia/h100-pcie
  nvidia/t4
  nvidia/a10g
  nvidia/l4
  nvidia/l40s
  amd/mi300x
  amd/mi325x
  amd/mi350x
)
set(_gen_dir "${CMAKE_BINARY_DIR}/generated")
set(_gen "${_gen_dir}/vgpu/embedded_profiles.hpp")
set(_content "// Generated by CMake from profiles/ — do not edit.\n#pragma once\n\nnamespace vgpu::embedded {\n\nstruct EmbeddedProfile {\n  const char* id;\n  const char* yaml;\n};\n\ninline constexpr EmbeddedProfile kProfiles[] = {\n")
foreach(_p IN LISTS VGPU_PROFILES)
  file(READ "${CMAKE_SOURCE_DIR}/profiles/${_p}.yaml" _yaml)
  string(APPEND _content "  {\"${_p}\", R\"VGPUPROFILE(${_yaml})VGPUPROFILE\"},\n")
  set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "profiles/${_p}.yaml")
endforeach()
string(APPEND _content "};\n\n}  // namespace vgpu::embedded\n")
file(WRITE "${_gen}" "${_content}")

# ---------------------------------------------------------------------------
# Core static library: profiles, memory, PTX, execution engine, runtime.
# ---------------------------------------------------------------------------
add_library(vgpu STATIC
  src/core/error.cpp
  src/core/yamlish.cpp
  src/core/profile.cpp
  src/core/registry.cpp
  src/core/memory.cpp
  src/core/telemetry.cpp
  src/core/profiling.cpp
  src/core/faults.cpp
  src/ptx/ast.cpp
  src/ptx/lexer.cpp
  src/ptx/parser.cpp
  src/ptx/regalloc.cpp
  src/exec/scheduler.cpp
  src/exec/interpreter.cpp
  src/runtime/runtime.cpp
)
target_include_directories(vgpu PUBLIC include "${_gen_dir}")

# libvgpucuda: the CUDA Driver API shim external programs link against.
add_library(vgpucuda SHARED src/cuda/driver_api.cpp src/cuda/fatbin.cpp)
target_link_libraries(vgpucuda PRIVATE vgpu ${CMAKE_DL_LIBS})
# Present the real driver's identity: apps linked with -lcuda record SONAME
# "libcuda.so.1" and resolve to this library through LD_LIBRARY_PATH at run time.
set_target_properties(vgpucuda PROPERTIES OUTPUT_NAME cuda SOVERSION 1 VERSION 1.1)
# The shim directory: prepend to LD_LIBRARY_PATH and unmodified CUDA binaries
# (static cudart included) dlopen VirtualGPU as the driver.
add_custom_command(TARGET vgpucuda POST_BUILD
  COMMAND ${CMAKE_COMMAND} -E make_directory "${CMAKE_BINARY_DIR}/shim"
  COMMAND ${CMAKE_COMMAND} -E create_symlink "../libcuda.so.1.1" "${CMAKE_BINARY_DIR}/shim/libcuda.so.1"
  COMMAND ${CMAKE_COMMAND} -E create_symlink "../libcuda.so.1.1" "${CMAKE_BINARY_DIR}/shim/libcuda.so"
  COMMENT "Creating driver shim symlinks (build/shim/libcuda.so.1)")

# ---------------------------------------------------------------------------
# libvgpucudart: the CUDA *runtime* API shim. Lets unmodified nvcc-compiled
# applications (built against shared cudart) run on VirtualGPU. Requires the
# CUDA toolkit's ABI headers at build time (driver_types.h) for exact struct
# layout; built only when they are found, so the core stays dependency-free.
# ---------------------------------------------------------------------------
# Derive the ABI include from the nvcc that will compile the workloads, so the
# shim's cudaDeviceProp layout matches the application's exactly. A mismatched
# system copy (e.g. an older /usr/include/driver_types.h) would corrupt caller
# structs, so nvcc's own toolkit include is the only trustworthy source.
find_program(VGPU_NVCC nvcc)
if(VGPU_NVCC)
  # Resolve symlinks first: distributions put an nvcc symlink in /usr/bin, and
  # deriving the toolkit root from that gives /usr, where no CUDA library lives.
  get_filename_component(_nvcc_real "${VGPU_NVCC}" REALPATH)
  get_filename_component(_nvcc_bin "${_nvcc_real}" DIRECTORY)
  get_filename_component(_cuda_root "${_nvcc_bin}" DIRECTORY)
  find_path(CUDA_ABI_INCLUDE driver_types.h
    PATHS "${_cuda_root}/include" "${_cuda_root}/targets/x86_64-linux/include"
    NO_DEFAULT_PATH)
endif()
if(NOT CUDA_ABI_INCLUDE)
  find_path(CUDA_ABI_INCLUDE driver_types.h
    PATHS /usr/local/cuda/include /opt/cuda/include ENV CUDA_HOME ENV CUDA_PATH
    PATH_SUFFIXES include NO_DEFAULT_PATH)
endif()
# The soname is what the loader matches on, and each CUDA library carries its
# own major version that moves between toolkits (cuBLAS is .so.12 under CUDA 12
# and .so.13 under CUDA 13). Building under the wrong one means programs
# silently load NVIDIA's real library instead of this one -- which is exactly
# what happened when cuRAND was built as .so.13. So rather than hard-code a
# table that goes stale, read the major off the toolkit that is actually
# installed, and fall back to the documented value if the library is absent.
function(vgpu_toolkit_soversion _name _default _out)
  set(_found "")
  # The include directory that was actually accepted is the most reliable
  # anchor: its sibling lib directories belong to the same toolkit.
  get_filename_component(_inc_root "${CUDA_ABI_INCLUDE}" DIRECTORY)
  # Distro-packaged toolkits (Ubuntu's cuda-* debs) put the headers in
  # /usr/include and the libraries in the multiarch dir, which none of the
  # toolkit-shaped paths above cover: the search fell through to the default
  # and built a .so.13 shim on a CUDA 12 host, where it is never loaded
  # because the program asks the loader for libcudart.so.12.
  foreach(_dir "${_cuda_root}/lib64" "${_cuda_root}/targets/x86_64-linux/lib" "${_cuda_root}/lib"
               "${_cuda_root}/lib/${CMAKE_LIBRARY_ARCHITECTURE}"
               "${_inc_root}/lib64" "${_inc_root}/lib"
               "${_inc_root}/lib/${CMAKE_LIBRARY_ARCHITECTURE}"
               "/usr/lib/${CMAKE_LIBRARY_ARCHITECTURE}" "/lib/${CMAKE_LIBRARY_ARCHITECTURE}")
    file(GLOB _cands "${_dir}/lib${_name}.so.[0-9]*")
    foreach(_c IN LISTS _cands)
      if("${_c}" MATCHES "\\.so\\.([0-9]+)$")
        set(_found "${CMAKE_MATCH_1}")
        break()
      endif()
    endforeach()
    if(_found)
      break()
    endif()
  endforeach()
  if(NOT _found)
    set(_found "${_default}")
  endif()
  set(${_out} "${_found}" PARENT_SCOPE)
endfunction()

# A version script naming the soname's own version node, which is how NVIDIA
# tags these symbols. Getting it wrong makes the loader warn about missing
# version information on every call.
function(vgpu_version_script _libname _soversion _out)
  set(_path "${CMAKE_BINARY_DIR}/maps/lib${_libname}.so.${_soversion}.map")
  file(WRITE "${_path}" "lib${_libname}.so.${_soversion} {\n  global: *;\n};\n")
  set(${_out} "${_path}" PARENT_SCOPE)
endfunction()

if(CUDA_ABI_INCLUDE)
  message(STATUS "CUDA ABI headers at ${CUDA_ABI_INCLUDE}: building libvgpucudart")
  vgpu_toolkit_soversion(cudart 13 SO_CUDART)
  message(STATUS "toolkit runtime soname: libcudart.so.${SO_CUDART}")
  add_library(vgpucudart SHARED src/cuda/runtime_api.cpp src/cuda/fatbin.cpp)
  target_link_libraries(vgpucudart PRIVATE vgpu ${CMAKE_DL_LIBS})
  target_include_directories(vgpucudart PRIVATE ${CUDA_ABI_INCLUDE} src/cuda)
  # Present as the toolkit's own libcudart soname so unmodified binaries find
  # us via LD_LIBRARY_PATH. Same identity trick as libcuda.
  vgpu_version_script(cudart ${SO_CUDART} _cudart_map)
  set_target_properties(vgpucudart PROPERTIES
    OUTPUT_NAME cudart SOVERSION ${SO_CUDART} VERSION ${SO_CUDART}.0)
  target_link_options(vgpucudart PRIVATE "-Wl,--version-script=${_cudart_map}")
  add_custom_command(TARGET vgpucudart POST_BUILD
    COMMAND ${CMAKE_COMMAND} -E make_directory "${CMAKE_BINARY_DIR}/shim"
    COMMAND ${CMAKE_COMMAND} -E create_symlink "../libcudart.so.${SO_CUDART}.0"
            "${CMAKE_BINARY_DIR}/shim/libcudart.so.${SO_CUDART}"
    COMMAND ${CMAKE_COMMAND} -E create_symlink "../libcudart.so.${SO_CUDART}.0"
            "${CMAKE_BINARY_DIR}/shim/libcudart.so"
    COMMENT "Creating runtime shim symlinks (build/shim/libcudart.so.${SO_CUDART})")
  # ---------------------------------------------------------------------------
  # libvgpunvml: NVML, presented as libnvidia-ml.so.1. nvidia-smi dlopens that
  # soname and resolves entries by name, so the real nvidia-smi binary reports
  # VirtualGPU's devices when the shim directory is on LD_LIBRARY_PATH.
  # ---------------------------------------------------------------------------
  add_library(vgpunvml SHARED src/cuda/nvml_api.cpp)
  target_link_libraries(vgpunvml PRIVATE vgpu)
  target_include_directories(vgpunvml PRIVATE ${CUDA_ABI_INCLUDE})
  set_target_properties(vgpunvml PROPERTIES OUTPUT_NAME nvidia-ml SOVERSION 1)
  add_custom_command(TARGET vgpunvml POST_BUILD
    COMMAND ${CMAKE_COMMAND} -E make_directory "${CMAKE_BINARY_DIR}/shim"
    COMMAND ${CMAKE_COMMAND} -E create_symlink "../libnvidia-ml.so.1"
            "${CMAKE_BINARY_DIR}/shim/libnvidia-ml.so.1"
    COMMAND ${CMAKE_COMMAND} -E create_symlink "../libnvidia-ml.so.1"
            "${CMAKE_BINARY_DIR}/shim/libnvidia-ml.so"
    COMMENT "Creating NVML shim symlinks (build/shim/libnvidia-ml.so.1)")

  # ---------------------------------------------------------------------------
  # libvgpucupti: CUPTI, which is how every profiler reads a CUDA program --
  # Nsight, nvprof, PyTorch's profiler, DCGM. Without it the counters and the
  # timeline this engine already has are reachable only through VGPU_COUNTERS,
  # which no existing tool knows how to read. See docs/cupti.md for what the
  # timestamps do and do not mean.
  # ---------------------------------------------------------------------------
  vgpu_toolkit_soversion(cupti 13 SO_CUPTI)
  vgpu_version_script(cupti ${SO_CUPTI} _cupti_map)
  add_library(vgpucupti SHARED src/cuda/cupti_api.cpp)
  # Against the runtime shim rather than the static core, deliberately. The
  # profiling switch and its event queue are process-global state: linking the
  # static library here would give this library its own copy, so CUPTI would
  # turn recording on in one and the runtime would keep reading the other, and
  # a profiler would attach successfully and receive nothing.
  target_link_libraries(vgpucupti PRIVATE vgpucudart)
  target_include_directories(vgpucupti PRIVATE ${CUDA_ABI_INCLUDE} include)
  set_target_properties(vgpucupti PROPERTIES
    OUTPUT_NAME cupti SOVERSION ${SO_CUPTI} VERSION ${SO_CUPTI}.0)
  target_link_options(vgpucupti PRIVATE "-Wl,--version-script=${_cupti_map}")
  add_custom_command(TARGET vgpucupti POST_BUILD
    COMMAND ${CMAKE_COMMAND} -E make_directory "${CMAKE_BINARY_DIR}/shim"
    COMMAND ${CMAKE_COMMAND} -E create_symlink "../libcupti.so.${SO_CUPTI}.0"
            "${CMAKE_BINARY_DIR}/shim/libcupti.so.${SO_CUPTI}"
    COMMAND ${CMAKE_COMMAND} -E create_symlink "../libcupti.so.${SO_CUPTI}.0"
            "${CMAKE_BINARY_DIR}/shim/libcupti.so"
    COMMENT "Creating CUPTI shim symlinks (build/shim/libcupti.so.${SO_CUPTI})")

  # ---------------------------------------------------------------------------
  # libvgpucublas: cuBLAS, presented as libcublas.so.13. cuBLAS is a library,
  # not user code, so its arithmetic runs on the host at native speed rather
  # than through the SIMT interpreter -- see docs/cublas.md.
  # ---------------------------------------------------------------------------
  vgpu_toolkit_soversion(cublas 13 SO_CUBLAS)
  vgpu_version_script(cublas ${SO_CUBLAS} _cublas_map)
  add_library(vgpucublas SHARED src/cuda/cublas_api.cpp src/cuda/generated/cublas_stubs.cpp)
  target_link_libraries(vgpucublas PRIVATE vgpu vgpucudart)
  target_include_directories(vgpucublas PRIVATE ${CUDA_ABI_INCLUDE})
  set_target_properties(vgpucublas PROPERTIES
    OUTPUT_NAME cublas SOVERSION ${SO_CUBLAS} VERSION ${SO_CUBLAS}.0)
  target_link_options(vgpucublas PRIVATE "-Wl,--version-script=${_cublas_map}")
  add_custom_command(TARGET vgpucublas POST_BUILD
    COMMAND ${CMAKE_COMMAND} -E make_directory "${CMAKE_BINARY_DIR}/shim"
    COMMAND ${CMAKE_COMMAND} -E create_symlink "../libcublas.so.${SO_CUBLAS}.0"
            "${CMAKE_BINARY_DIR}/shim/libcublas.so.${SO_CUBLAS}"
    COMMAND ${CMAKE_COMMAND} -E create_symlink "../libcublas.so.${SO_CUBLAS}.0"
            "${CMAKE_BINARY_DIR}/shim/libcublas.so"
    COMMENT "Creating cuBLAS shim symlinks (build/shim/libcublas.so.${SO_CUBLAS})")

  # ---------------------------------------------------------------------------
  # Additional vendor libraries. Same boundary as cuBLAS: library math runs on
  # the host, application kernels are simulated. Each is presented under the
  # real soname so unmodified programs link against it.
  # ---------------------------------------------------------------------------
  # The second field is only the fallback: the real major is read off the
  # installed toolkit, because it moves between CUDA releases (cuFFT is .so.11
  # under CUDA 12 and .so.12 under CUDA 13).
  set(_vgpu_libs
    "curand:10:curand_api"
    "cublasLt:13:cublaslt_api"
    "cufft:12:cufft_api"
    "cusparse:12:cusparse_api"
    "cusolver:12:cusolver_api"
    "nvrtc:13:nvrtc_api"
    "nvjpeg:13:nvjpeg_api"
  )
  foreach(_entry IN LISTS _vgpu_libs)
    string(REPLACE ":" ";" _parts "${_entry}")
    list(GET _parts 0 _lib)
    list(GET _parts 1 _fallback)
    list(GET _parts 2 _src)
    vgpu_toolkit_soversion(${_lib} ${_fallback} _ver)
    vgpu_version_script(${_lib} ${_ver} _map)
    string(TOLOWER "${_lib}" _lower)
    # Some of these libraries also carry generated stubs for the entry points
    # they export but VirtualGPU does not implement: a framework resolves every
    # symbol at load time, so one missing name blocks the import outright.
    set(_extra_src "")
    if(EXISTS "${CMAKE_SOURCE_DIR}/src/cuda/generated/${_lower}_stubs.cpp")
      set(_extra_src "src/cuda/generated/${_lower}_stubs.cpp")
    endif()
    add_library(vgpu${_lower} SHARED src/cuda/${_src}.cpp ${_extra_src})
    target_link_libraries(vgpu${_lower} PRIVATE vgpu vgpucudart)
    target_include_directories(vgpu${_lower} PRIVATE ${CUDA_ABI_INCLUDE})
    set_target_properties(vgpu${_lower} PROPERTIES
      OUTPUT_NAME ${_lib} SOVERSION ${_ver} VERSION ${_ver}.0)
    target_link_options(vgpu${_lower} PRIVATE "-Wl,--version-script=${_map}")
    add_custom_command(TARGET vgpu${_lower} POST_BUILD
      COMMAND ${CMAKE_COMMAND} -E make_directory "${CMAKE_BINARY_DIR}/shim"
      COMMAND ${CMAKE_COMMAND} -E create_symlink "../lib${_lib}.so.${_ver}.0"
              "${CMAKE_BINARY_DIR}/shim/lib${_lib}.so.${_ver}"
      COMMAND ${CMAKE_COMMAND} -E create_symlink "../lib${_lib}.so.${_ver}.0"
              "${CMAKE_BINARY_DIR}/shim/lib${_lib}.so"
      COMMENT "Creating ${_lib} shim symlinks (soname lib${_lib}.so.${_ver})")
  endforeach()

  # ---------------------------------------------------------------------------
  # NPP. NVIDIA splits it across eleven shared objects that share one API
  # surface, so one source is built into each under its own soname. The symbols
  # are identical in every copy and ELF resolves a name to a single definition
  # process-wide, so the duplication costs disk and nothing else.
  # ---------------------------------------------------------------------------
  set(_vgpu_npp_libs nppc nppial nppicc nppidei nppif nppig nppim nppist nppisu nppitc npps)
  foreach(_npp IN LISTS _vgpu_npp_libs)
    vgpu_toolkit_soversion(${_npp} 13 _nppver)
    vgpu_version_script(${_npp} ${_nppver} _nppmap)
    add_library(vgpu${_npp} SHARED src/cuda/npp_api.cpp)
    target_link_libraries(vgpu${_npp} PRIVATE vgpu vgpucudart)
    target_include_directories(vgpu${_npp} PRIVATE ${CUDA_ABI_INCLUDE})
    set_target_properties(vgpu${_npp} PROPERTIES
      OUTPUT_NAME ${_npp} SOVERSION ${_nppver} VERSION ${_nppver}.0)
    target_link_options(vgpu${_npp} PRIVATE "-Wl,--version-script=${_nppmap}")
    add_custom_command(TARGET vgpu${_npp} POST_BUILD
      COMMAND ${CMAKE_COMMAND} -E make_directory "${CMAKE_BINARY_DIR}/shim"
      COMMAND ${CMAKE_COMMAND} -E create_symlink "../lib${_npp}.so.${_nppver}.0"
              "${CMAKE_BINARY_DIR}/shim/lib${_npp}.so.${_nppver}"
      COMMAND ${CMAKE_COMMAND} -E create_symlink "../lib${_npp}.so.${_nppver}.0"
              "${CMAKE_BINARY_DIR}/shim/lib${_npp}.so"
      COMMENT "Creating ${_npp} shim symlinks (soname lib${_npp}.so.${_nppver})")
  endforeach()

  # ---------------------------------------------------------------------------
  # libvgpunccl: NCCL, presented as libnccl.so.2. There is no NVLink to move
  # bytes over, so the transport is a directory of memory-mapped files -- which
  # is what lets one-rank-per-process launchers (torchrun) work unmodified.
  # ---------------------------------------------------------------------------
  find_path(NCCL_INCLUDE nccl.h
    PATHS "${CMAKE_SOURCE_DIR}/third_party/nccl_include" ENV NCCL_HEADER_DIR
    NO_DEFAULT_PATH)
  if(NCCL_INCLUDE)
    message(STATUS "NCCL headers at ${NCCL_INCLUDE}: building libvgpunccl")
    add_library(vgpunccl SHARED src/cuda/nccl_api.cpp src/cuda/generated/nccl_stubs.cpp)
    target_link_libraries(vgpunccl PRIVATE vgpu vgpucudart)
    target_include_directories(vgpunccl PRIVATE ${NCCL_INCLUDE} ${CUDA_ABI_INCLUDE})
    set_target_properties(vgpunccl PROPERTIES OUTPUT_NAME nccl SOVERSION 2 VERSION 2.0)
    add_custom_command(TARGET vgpunccl POST_BUILD
      COMMAND ${CMAKE_COMMAND} -E make_directory "${CMAKE_BINARY_DIR}/shim"
      COMMAND ${CMAKE_COMMAND} -E create_symlink "../libnccl.so.2.0"
              "${CMAKE_BINARY_DIR}/shim/libnccl.so.2"
      COMMAND ${CMAKE_COMMAND} -E create_symlink "../libnccl.so.2.0"
              "${CMAKE_BINARY_DIR}/shim/libnccl.so"
      COMMENT "Creating NCCL shim symlinks (build/shim/libnccl.so.2)")
  else()
    message(STATUS "NCCL headers not found: skipping libvgpunccl")
  endif()

  # ---------------------------------------------------------------------------
  # libvgpucudnn: cuDNN, presented as libcudnn.so.9. Built against the real
  # vendored headers (third_party/cudnn_include) -- hand-writing a vendor
  # struct layout has silently produced garbage twice, so we never do it.
  # Real cuDNN exports versioned symbols tagged "libcudnn.so.9"; the version
  # script gives ours the same tag so the loader binds without the
  # "no version information available" warning.
  # ---------------------------------------------------------------------------
  find_path(CUDNN_INCLUDE cudnn.h
    PATHS "${CMAKE_SOURCE_DIR}/third_party/cudnn_include" ENV CUDNN_HEADER_DIR
    NO_DEFAULT_PATH)
  if(CUDNN_INCLUDE)
    message(STATUS "cuDNN headers at ${CUDNN_INCLUDE}: building libvgpucudnn")
    add_library(vgpucudnn SHARED src/cuda/cudnn_api.cpp src/cuda/generated/cudnn_stubs.cpp)
    target_link_libraries(vgpucudnn PRIVATE vgpu vgpucudart)
    target_include_directories(vgpucudnn PRIVATE ${CUDNN_INCLUDE} ${CUDA_ABI_INCLUDE})
    set_target_properties(vgpucudnn PROPERTIES OUTPUT_NAME cudnn SOVERSION 9 VERSION 9.0)
    target_link_options(vgpucudnn PRIVATE
      "-Wl,--version-script=${CMAKE_SOURCE_DIR}/src/cuda/cudnn.map")
    add_custom_command(TARGET vgpucudnn POST_BUILD
      COMMAND ${CMAKE_COMMAND} -E make_directory "${CMAKE_BINARY_DIR}/shim"
      COMMAND ${CMAKE_COMMAND} -E create_symlink "../libcudnn.so.9.0"
              "${CMAKE_BINARY_DIR}/shim/libcudnn.so.9"
      COMMAND ${CMAKE_COMMAND} -E create_symlink "../libcudnn.so.9.0"
              "${CMAKE_BINARY_DIR}/shim/libcudnn.so"
      COMMENT "Creating cuDNN shim symlinks (build/shim/libcudnn.so.9)")
  else()
    message(STATUS "cuDNN headers not found: skipping libvgpucudnn")
  endif()

  # ---------------------------------------------------------------------------
  # libvgpunvenc: VirtualGPU's NVENC implementation, presented as
  # libnvidia-encode.so.1. Applications dlopen that bare soname, which searches
  # LD_LIBRARY_PATH first, so the shim directory wins over the real driver
  # library. Needs the Video Codec SDK header (vendored by the workload tree).
  # ---------------------------------------------------------------------------
  find_path(NVENC_INCLUDE nvEncodeAPI.h
    PATHS "${CMAKE_SOURCE_DIR}/../pantheongpu/kernels/common/nvenc"
          ENV NVENC_HEADER_DIR)
  if(NVENC_INCLUDE)
    message(STATUS "NVENC header at ${NVENC_INCLUDE}: building libvgpunvenc")
    add_library(vgpunvenc SHARED src/cuda/nvenc_api.cpp)
    target_link_libraries(vgpunvenc PRIVATE vgpu vgpucudart)
    target_include_directories(vgpunvenc PRIVATE ${NVENC_INCLUDE})
    set_target_properties(vgpunvenc PROPERTIES OUTPUT_NAME nvidia-encode SOVERSION 1)
    add_custom_command(TARGET vgpunvenc POST_BUILD
      COMMAND ${CMAKE_COMMAND} -E make_directory "${CMAKE_BINARY_DIR}/shim"
      COMMAND ${CMAKE_COMMAND} -E create_symlink "../libnvidia-encode.so.1"
              "${CMAKE_BINARY_DIR}/shim/libnvidia-encode.so.1"
      COMMENT "Creating NVENC shim symlink (build/shim/libnvidia-encode.so.1)")
  else()
    message(STATUS "nvEncodeAPI.h not found: skipping libvgpunvenc")
  endif()
else()
  message(STATUS "CUDA ABI headers not found: skipping libvgpucudart (core unaffected)")
endif()

# CLI
add_executable(vgpu_cli src/cli/main.cpp src/cli/demo.cpp src/cli/smi.cpp src/cli/serve.cpp src/cli/shell.cpp src/cli/run.cpp src/cli/test.cpp)
target_link_libraries(vgpu_cli PRIVATE vgpu)
set_target_properties(vgpu_cli PROPERTIES OUTPUT_NAME vgpu)
# Drop-in monitoring commands. On a CPU-only machine nvidia-smi / rocm-smi /
# rocm_agent_enumerator do not exist at all, so VirtualGPU supplies them; they
# render the same telemetry the NVML shim serves.
add_custom_command(TARGET vgpu_cli POST_BUILD
  COMMAND ${CMAKE_COMMAND} -E make_directory "${CMAKE_BINARY_DIR}/bin"
  COMMAND ${CMAKE_COMMAND} -E create_symlink "../vgpu" "${CMAKE_BINARY_DIR}/bin/vgpu"
  COMMAND ${CMAKE_COMMAND} -E copy "${CMAKE_SOURCE_DIR}/tools/nvidia-smi" "${CMAKE_BINARY_DIR}/bin/nvidia-smi"
  COMMAND ${CMAKE_COMMAND} -E copy "${CMAKE_SOURCE_DIR}/tools/nvcc" "${CMAKE_BINARY_DIR}/bin/nvcc"
  COMMAND ${CMAKE_COMMAND} -E copy "${CMAKE_SOURCE_DIR}/tools/rocm-smi" "${CMAKE_BINARY_DIR}/bin/rocm-smi"
  COMMAND ${CMAKE_COMMAND} -E copy "${CMAKE_SOURCE_DIR}/tools/rocm_agent_enumerator" "${CMAKE_BINARY_DIR}/bin/rocm_agent_enumerator"
  COMMENT "Installing drop-in monitoring tools into build/bin")

# ---------------------------------------------------------------------------
# Tests
# ---------------------------------------------------------------------------
enable_testing()
function(vgpu_unit_test name)
  add_executable(${name} tests/unit/${name}.cpp)
  target_link_libraries(${name} PRIVATE vgpu)
  target_include_directories(${name} PRIVATE tests/framework)
  add_test(NAME ${name} COMMAND ${name})
endfunction()

vgpu_unit_test(test_yamlish)
vgpu_unit_test(test_profile)
vgpu_unit_test(test_memory)
vgpu_unit_test(test_ptx)
target_compile_definitions(test_ptx PRIVATE VGPU_KERNEL_DIR="${CMAKE_SOURCE_DIR}/tests/kernels")
vgpu_unit_test(test_exec)
target_compile_definitions(test_exec PRIVATE VGPU_KERNEL_DIR="${CMAKE_SOURCE_DIR}/tests/kernels")
vgpu_unit_test(test_runtime)
vgpu_unit_test(test_exec2)
vgpu_unit_test(test_exec3)
vgpu_unit_test(test_telemetry)
vgpu_unit_test(test_regalloc)
vgpu_unit_test(test_robustness)
target_include_directories(test_robustness PRIVATE src/cuda)
target_sources(test_robustness PRIVATE src/cuda/fatbin.cpp)
target_link_libraries(test_robustness PRIVATE ${CMAKE_DL_LIBS})

# The C harness: an "external application" built with a plain C compiler
# against the documented ABI only (vgpu_cuda.h + libvgpucuda.so).
add_executable(vector_add_driver tests/c_harness/vector_add_driver.c)
target_link_libraries(vector_add_driver PRIVATE vgpucuda)
target_include_directories(vector_add_driver PRIVATE include)
add_test(NAME c_harness_vector_add COMMAND vector_add_driver)
set_tests_properties(c_harness_vector_add PROPERTIES ENVIRONMENT "VGPU_GPU=nvidia/h100")

# nvidia-smi's table is scraped positionally by real tooling, so its geometry
# is pinned against a physical driver's output rather than left to drift.
add_test(NAME smi_format COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_smi_format.sh)
set_tests_properties(smi_format PROPERTIES ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}")

# With no `unshare`, vgpu shell must not touch the host's mounts.
add_test(NAME isolation_fallback COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_isolation_fallback.sh)
set_tests_properties(isolation_fallback PROPERTIES ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}")

# VGPU_QUIET=1 silences and nothing else does. Skips itself without the cuFFT shim.
add_test(NAME quiet_semantics COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_quiet.sh)
set_tests_properties(quiet_semantics PROPERTIES ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}")

# The simulator's reports agree with each other and with real cards: one CUDA
# version across the driver API, the runtime API and nvidia-smi; and two memory
# totals, framebuffer and totalGlobalMem, that differ by the card's reserve.
if(TARGET vgpucudart)
  add_test(NAME driver_version COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_driver_version.sh)
  set_tests_properties(driver_version PROPERTIES DEPENDS vgpucudart
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}")
  add_test(NAME memory_totals COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_memory_totals.sh)
  set_tests_properties(memory_totals PROPERTIES DEPENDS vgpucudart
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}")
endif()

# End-to-end: an nvcc-compiled CUDA app run through libvgpucudart. Depends on
# the runtime shim; only meaningful when it was built (CUDA headers present).
if(TARGET vgpucudart)
  add_test(NAME e2e_vector_add COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_e2e.sh)
  set_tests_properties(e2e_vector_add PROPERTIES DEPENDS vgpucudart
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}")
endif()

# One rank per process over the file-backed NCCL transport. This is the shape
# real launchers use, and the only test where the ranks share no address space.
if(TARGET vgpunccl)
  add_test(NAME e2e_nccl_multiproc
           COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_nccl_multiproc.sh 4)
  set_tests_properties(e2e_nccl_multiproc PROPERTIES DEPENDS vgpunccl TIMEOUT 300
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}")
  add_test(NAME e2e_nccl_group COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_nccl_group.sh 4)
  set_tests_properties(e2e_nccl_group PROPERTIES DEPENDS vgpunccl TIMEOUT 300
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}")
endif()

# A cuBLAS call captured into a CUDA graph. The library computes on the host,
# which is invisible to a program until it captures a graph: the call has to be
# recorded and run at launch, over what the graph's own kernels produced, not
# computed eagerly over operands that do not exist yet.
if(TARGET vgpucublas)
  add_test(NAME e2e_graph_cublas
           COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_graph_cublas.sh)
  set_tests_properties(e2e_graph_cublas PROPERTIES
    DEPENDS vgpucublas
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
    TIMEOUT 300)

  # A leading dimension pads between columns, not after the last one. Operands
  # allocated to exactly their extent make an over-read fault instead of
  # quietly picking up whatever followed them.
  add_test(NAME e2e_cublas_tight
           COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_cublas_tight.sh)
  set_tests_properties(e2e_cublas_tight PROPERTIES
    DEPENDS vgpucublas
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
    TIMEOUT 300)

  # Where each lane's mma.sync fragment element sits. A permuted mapping
  # cancels out wherever a fragment is written back the way it was read, so
  # this pins it against the ISA rather than against a matmul's answer.
  add_test(NAME e2e_mma_layout
           COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_mma_layout.sh)
  set_tests_properties(e2e_mma_layout PROPERTIES
    DEPENDS vgpucudart
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
    TIMEOUT 300)

  # bf16 arithmetic, half2 atomics and labeled_partition, through nvcc. The
  # unit tests feed hand-written PTX; a compiler emits forms a hand-written
  # test never thinks to write, and this is what catches the difference. It
  # already has: nvcc lowers labeled_partition through bfind.shiftamt, which
  # nothing else here would have asked for.
  # A full 16x16x16 matmul through the public WMMA API: load both operands and
  # the accumulator, mma, store. The fragment layout is opaque by design -- the
  # only observable contract is the product -- so this checks it against a host
  # reference rather than against a register assignment.
  add_test(NAME e2e_wmma_gemm
           COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_wmma_gemm.sh)
  set_tests_properties(e2e_wmma_gemm PROPERTIES
    DEPENDS vgpucudart
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
    TIMEOUT 300)

  # Intrinsics and builtins the PTX probe turned up: atomicInc/Dec's wrapping,
  # the per-byte SIMD video ops, device malloc/free, the half2 math family
  # (whose inline asm declares ".reg.b16" with no space), and a device assert
  # reporting cudaErrorAssert with its message through cudaDeviceSynchronize.
  # Non-inlined device functions -- a real call frame, with divergence inside
  # the callee and recursion, which cannot be inlined at all.
  # __constant__ and __device__ variables written and read from the host. The
  # kernel side needs ld.const; the host side needs the symbol API, which
  # __cudaRegisterVar has to feed.
  # The same programs built with -rdc=true. A separately compiled build takes a
  # different loading path entirely -- an empty primary fatbin plus a list of
  # relocatable ones the runtime has to link -- so any of these failing here
  # and passing normally means the linking is wrong, not the program.
  # Thrust and CUB device-level algorithms, unmodified. Neither is a thin
  # wrapper -- a radix sort is a tuned multi-kernel pipeline with its own
  # temporary storage, decoupled look-back scan and warp primitives throughout
  # -- so running them end to end exercises far more than a hand-written kernel
  # does, and the answer is checked rather than the launch's return code.
  add_test(NAME e2e_libraries
           COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_libraries.sh)
  set_tests_properties(e2e_libraries PROPERTIES
    DEPENDS vgpucudart
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
    TIMEOUT 600)

  add_test(NAME e2e_rdc_link
           COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_rdc_link.sh)
  set_tests_properties(e2e_rdc_link PROPERTIES
    DEPENDS vgpucudart
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
    TIMEOUT 300)

  add_test(NAME e2e_symbols
           COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_symbols.sh)
  set_tests_properties(e2e_symbols PROPERTIES
    DEPENDS vgpucudart
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
    TIMEOUT 300)

  add_test(NAME e2e_device_functions
           COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_device_functions.sh)
  set_tests_properties(e2e_device_functions PROPERTIES
    DEPENDS vgpucudart
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
    TIMEOUT 300)

  add_test(NAME e2e_device_intrinsics
           COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_device_intrinsics.sh)
  set_tests_properties(e2e_device_intrinsics PROPERTIES
    DEPENDS vgpucudart
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
    TIMEOUT 300)

  add_test(NAME e2e_modern_dtypes
           COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_modern_dtypes.sh)
  set_tests_properties(e2e_modern_dtypes PROPERTIES
    DEPENDS vgpucudart
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
    TIMEOUT 300)
endif()

# A program profiling itself through CUPTI, which is how every profiler reads a
# CUDA program. Checks the records describe the work that was actually issued.
if(TARGET vgpucupti)
  add_test(NAME e2e_cupti_activity
           COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_cupti_activity.sh)
  set_tests_properties(e2e_cupti_activity PROPERTIES
    DEPENDS vgpucupti
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
    TIMEOUT 300)

  # And the same thing through a tool nobody here wrote.
  add_test(NAME e2e_nvprof COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_nvprof.sh)
  set_tests_properties(e2e_nvprof PROPERTIES
    DEPENDS vgpucupti
    ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
    TIMEOUT 600)
endif()

# Texture and surface objects: tex1Dfetch over linear memory, tex2D over a
# cudaArray, a surface read-modify-write, and the linear-filter refusal.
add_test(NAME e2e_textures COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_textures.sh)
set_tests_properties(e2e_textures PROPERTIES
  DEPENDS vgpucudart
  ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
  TIMEOUT 600)

# A grid-wide barrier through cooperative_groups, unmodified. Checks the
# barrier holds, that a grid too large to be resident is refused rather than
# hung, and that the same kernel launched ordinarily fails loudly.
add_test(NAME e2e_cooperative_grid
         COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_cooperative_grid.sh)
set_tests_properties(e2e_cooperative_grid PROPERTIES
  DEPENDS vgpucudart
  ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
  TIMEOUT 600)

# The shim compiled against NVIDIA's own cuda.h. This is the only test that can
# see a signature or an enum value that has drifted from the toolkit -- the shim
# is written against a clean-room header, so nothing else in the build compares
# the two. It existed unregistered for a while, which is how ten device
# attributes came to be filed under the wrong numbers.
add_test(NAME e2e_driver_abi COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_driver_abi.sh)
set_tests_properties(e2e_driver_abi PROPERTIES
  DEPENDS vgpucuda
  ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
  TIMEOUT 600)

# The `vgpu run` wrapper, and the two pre-flight checks that are the reason it
# exists: a CUDA soname the shim does not carry, and a DT_RPATH that outranks
# LD_LIBRARY_PATH. Both are silent failures without it.
add_test(NAME e2e_vgpu_run COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_vgpu_run.sh)
set_tests_properties(e2e_vgpu_run PROPERTIES
  DEPENDS vgpu_cli
  ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
  TIMEOUT 600)

# Numba and Triton compiling Python straight to kernels that run here. Neither
# framework uses cudart, so neither is reached by any other test: Numba goes
# through the driver's JIT link API and Triton through PTX handed over before
# ptxas sees it. Skips when the frameworks are not installed; set VGPU_PY to an
# interpreter that has them.
add_test(NAME e2e_jit_frameworks
         COMMAND ${CMAKE_SOURCE_DIR}/tests/e2e/run_jit_frameworks.sh)
set_tests_properties(e2e_jit_frameworks PROPERTIES
  ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
  TIMEOUT 1800)

# Real pantheon workloads, built by their own Makefile and run end to end. The
# unit tests cover instructions one at a time; this covers an unmodified GPU
# diagnostic actually working, which is where every serious bug here showed up
# first. Skips cleanly when no pantheon checkout is present, so a contributor
# without one still gets a green run.
add_test(NAME pantheon_workloads
         COMMAND ${CMAKE_SOURCE_DIR}/tests/workloads/run_pantheon_workloads.sh)
set_tests_properties(pantheon_workloads PROPERTIES
  ENVIRONMENT "VGPU_BUILD_DIR=${CMAKE_BINARY_DIR}"
  TIMEOUT 1800)
