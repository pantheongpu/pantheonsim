# VirtualGPU device profile: NVIDIA GH200 480GB
# Generated by nvidia/tools/characterize.cu from a physical device.
# Every value below was read from the hardware, not a datasheet.
id: nvidia/gh200-480gb
vendor: nvidia
model: "NVIDIA GH200 480GB"
architecture: hopper
compute_capability: "9.0"
warp_size: 32
vram_bytes: 101468602368
verified: true
limits:
  max_threads_per_block: 1024
  max_block_dim: [1024, 1024, 64]
  max_grid_dim: [2147483647, 65535, 65535]
  shared_mem_per_block_bytes: 49152
  shared_mem_per_block_optin_bytes: 232448
  registers_per_block: 65536
  multiprocessors: 132
  registers_per_sm: 65536
  max_threads_per_sm: 2048
  max_blocks_per_sm: 32
  max_registers_per_thread: 255
  shared_mem_per_sm: 233472
features:
  fp16: true
  bf16: true
  fp64: true
  tensor_cores: true
telemetry:
  framebuffer_mb: 97871          # nvidia-smi total on real cards (302 runs in the benchmark database)
  power_limit_w: 900
  sm_clock_max_mhz: 1980
  mem_clock_max_mhz: 2619
  # temperature_max_c: this driver did not report a thermal threshold
  pci_vendor_id: 4318          # 0x10DE
  pci_device_id: 9026          # 0x2342
  ecc: true                    # supported and on by default (NVIDIA datasheet)
  memory: hbm                  # rows are remapped on failure
  memory_temperature: false    # most real runs report no memory sensor
  # The Hopper die reaches its Grace CPU over NVLink-C2C; the PCIe link the
  # driver reports is x1.
  pcie_link: "Gen4 x1"         # most common link on real cards (365 runs in the benchmark database)
