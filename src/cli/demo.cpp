// vgpu demo vectoradd — stub until the execution engine (M5/M6) lands.
#include <cstdio>
#include <string>

int demo_vectoradd(const std::string& gpu, long long n) {
  (void)gpu;
  (void)n;
  std::fprintf(stderr, "vgpu demo vectoradd: not implemented yet (execution engine pending)\n");
  return 1;
}
