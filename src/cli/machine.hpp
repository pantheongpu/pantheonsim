// The machine a command describes (vgpu/machine.hpp), by the name the
// commands use.
#pragma once

#include "vgpu/machine.hpp"

namespace vgpu::cli {
using vgpu::read_machine;
}  // namespace vgpu::cli
