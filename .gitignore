build/
__pycache__/
*.o
# The code objects the AMD tests read are built artifacts on purpose: they are
# what a compiler produced, checked in so the tests need no compiler
# (amd/tests/data/build.sh rebuilds them).
!amd/tests/data/*.gfx942.o
*.so
compile_commands.json
.cache/
build-cu12/
build-fix/
build-san/
build-tsan/
